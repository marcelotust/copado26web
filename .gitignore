# Dependencies
node_modules
supabase/.temp/

# Claude Code — share per-repo skills/agents/commands; keep per-dev state local
.claude/settings.local.json
.claude/worktrees/

# Build output
/dist/
playwright-report
test-results
e2e/.auth/

# Local-only preview pages (brand kit, milestones, etc.) — not shipped to production
public/*-preview.html

# Environment variables
.env
.env.local
.env.*.local

# Editor
.vscode
.idea

# OS
.DS_Store
